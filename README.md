# rust-kernels

Reusable, independently testable and benchmarkable Rust algorithms and data structures.

The repository starts with spatial and collision kernels and is intended to grow through evidence-driven reuse across projects.
