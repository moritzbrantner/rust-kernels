[package]
name = "spatial-kernels"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Small deterministic spatial and collision kernels"

[dependencies]
