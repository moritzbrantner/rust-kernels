[package]
name = "rust-kernels-web-demo"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false
description = "Tiny WebAssembly adapter used by the rust-kernels GitHub Pages demo"

[lib]
crate-type = ["cdylib"]

[dependencies]
collection-kernels = { path = "../collection-kernels" }
