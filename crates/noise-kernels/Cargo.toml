[package]
name = "noise-kernels"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deterministic allocation-free procedural noise kernels"

[dependencies]

[dev-dependencies]
divan = "=0.1.21"

[[bench]]
name = "perlin"
harness = false
