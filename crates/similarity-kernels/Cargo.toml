[package]
name = "similarity-kernels"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Dependency-light exact similarity and sequence distance kernels"

[dependencies]
