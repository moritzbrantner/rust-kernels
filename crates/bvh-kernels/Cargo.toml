[package]
name = "bvh-kernels"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Small deterministic bounding-volume hierarchy kernels"

[dependencies]
spatial-kernels = { path = "../spatial-kernels" }
